spring:
  application:
    name: ms-users
  datasource:
    url: jdbc:mysql://localhost:3306/lottery
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082

